<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/act_adicionar"
        android:title="@string/act_Adicionar"
        android:icon="@android:drawable/ic_menu_add"
        app:showAsAction="ifRoom|withText"/>

    <item
        android:id="@+id/act_editar"
        android:title="@string/act_editar"
        android:icon="@android:drawable/ic_menu_edit"
        app:showAsAction="ifRoom|withText"/>

    <item
        android:id="@+id/act_deletar"
        android:title="@string/act_deletar"
        android:icon="@android:drawable/ic_menu_delete"
        app:showAsAction="ifRoom|withText"/>

    <item
        android:id="@+id/act_help"
        android:icon="@android:drawable/ic_menu_help"
        android:title="@string/act_help"
        app:showAsAction="never"/>

</menu>